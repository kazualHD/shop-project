import translation from './loginForm'

const reqiredMessage = translation.login_Validaros.required;
const emailValidMessage = translation.login_Validaros.email.email;
const passwordMinLength = translation.login_Validaros.passwors.minLength;
const passwordMaxLength = translation.login_Validaros.passwors.maxLength;


export default  {
    reqiredMessage,
    emailValidMessage,
    passwordMinLength,
    passwordMaxLength,
}